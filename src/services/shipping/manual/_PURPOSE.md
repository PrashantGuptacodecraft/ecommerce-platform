# Phase 1 ShippingProvider implementation
Flat rate + free-shipping threshold (calculateRate), manual courier/tracking
entry (createShipment/trackShipment just persist what the admin typed).
Implements only the ShippingProvider methods Phase 1 needs.
