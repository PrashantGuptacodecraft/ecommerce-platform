# (storefront) route group
Public, unauthenticated pages. Shared chrome (announcement bar, header,
mobile drawer nav, footer) lives in this group's layout.tsx.
Server Components by default; only interactive widgets (variant selector,
gallery, drawers) are Client Components.
