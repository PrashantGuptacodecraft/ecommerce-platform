# Central configuration
brand.ts (name, palette placeholder, contact info -- swap for real branding
without touching components), navigation.ts, design-tokens.ts (colours,
type scale, spacing, radii, shadows, container widths, z-index),
motion-tokens.ts (durations, easings, stagger delay), site.ts (site-wide
constants like max upload size, seed product cap).
